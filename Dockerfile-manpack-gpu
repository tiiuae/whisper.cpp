# Build stage
FROM nvcr.io/nvidia/l4t-base:r35.4.1 AS build

ENV DEBIAN_FRONTEND=noninteractive

# Install system dependencies
RUN apt-get update && apt-get install -y \
    git \
    cmake \
    build-essential \
    curl \
    wget \
    unzip \
    python3 \
    python3-pip \
    sox \
    ffmpeg \
    alsa-utils \
    libasound2-dev \
    libsdl2-dev \
    libssl-dev \
    libtool \
    automake \
    pkg-config \
    libopenblas-dev \
    libpulse-dev \
    pulseaudio \
    cuda-toolkit-10-2 \
    && rm -rf /var/lib/apt/lists/*

# Create working directory
WORKDIR /root/app

# Clone your specific repository and branch
RUN git clone -b feat/manpack https://github.com/tiiuae/whisper.cpp.git && \
    cd whisper.cpp && \
    git submodule update --init --recursive

# Install NATS.c
RUN git clone https://github.com/nats-io/nats.c.git && \
    cd nats.c && \
    mkdir build && \
    cd build && \
    cmake .. \
        -DNATS_BUILD_WITH_TLS=ON \
        -DNATS_BUILD_EXAMPLES=ON \
        -DBUILD_SHARED_LIBS=ON && \
    make -j2 && \
    make install && \
    cd ../.. && \
    rm -rf nats.c && \
    ldconfig

# Build whisper.cpp with CUDA and ARM optimizations
WORKDIR /root/app/whisper.cpp
RUN mkdir -p build && \
    cd build && \
    cmake -DWHISPER_SDL2=ON \
          -DCMAKE_BUILD_TYPE=Release \
          -DWHISPER_ARM_NEON=ON \
          -DWHISPER_OPENBLAS=ON \
          -DGGML_CUDA=ON \
          -DGGML_NATIVE=OFF \
          -DGGML_CPU_ARM_ARCH=armv8-a \
          -DCMAKE_CUDA_ARCHITECTURES="53;62;72;87" \
          .. && \
    make -j2

# Download the model
RUN cd models && ./download-ggml-model.sh tiny.en

# Runtime stage
FROM nvcr.io/nvidia/l4t-base:r35.4.1

ENV DEBIAN_FRONTEND=noninteractive

# Install runtime dependencies
RUN apt-get update && apt-get install -y \
    ffmpeg \
    libsdl2-2.0-0 \
    libgomp1 \
    libssl3 \
    libopenblas-base \
    libpulse0 \
    pulseaudio \
    alsa-utils \
    cuda-runtime-10-2 \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /root/app

# Copy only necessary files from build stage
COPY --from=build /root/app/whisper.cpp /root/app/whisper.cpp
COPY --from=build /usr/local/lib/libnats.so* /usr/local/lib/

# Update library cache
RUN ldconfig

# Set environment variables
ENV PATH="/root/app/whisper.cpp/build/bin:${PATH}"
ENV NATS_URL="nats://192.168.200.2:4222"

# Default command with correct path
CMD ["/root/app/whisper.cpp/build/bin/whisper-command", "-m", "/root/app/whisper.cpp/models/ggml-tiny.en.bin", "--capture", "0"]
